# Password Generator

A simple Python script that generates a random password based on the length the user chooses.

## How It Works

- Uses letters, numbers, and symbols
- Asks the user for password length
- Generates a secure random password

## How to Run

1. Open the folder in VS Code
2. Run 'main.py'
3. Enter the desired password length